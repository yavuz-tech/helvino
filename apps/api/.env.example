# ══════════════════════════════════════════════════════════════════
# Helvino API — Environment Variables
# ══════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values.
# NEVER commit .env (it contains secrets).
# ══════════════════════════════════════════════════════════════════

# ── Server ──
PORT=4000
HOST=0.0.0.0
NODE_ENV=development

# ── Security ──
SESSION_SECRET=
INTERNAL_API_KEY=
ORG_TOKEN_SECRET=
SIGNED_LINK_SECRET=
HCAPTCHA_SECRET_KEY=

# ── Trusted Hosts (comma-separated) ──
TRUSTED_HOSTS=
TRUSTED_PROXIES=

# ── Database ──
DATABASE_URL=

# ── Redis ──
REDIS_URL=redis://localhost:6379

# ── Public URLs ──
APP_PUBLIC_URL=http://localhost:3000
NEXT_PUBLIC_WEB_URL=http://localhost:3000
# ── CORS Allowed Origins (comma-separated, prod) ──
ALLOWED_ORIGINS=

# ── Stripe (Billing) ──
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO=
STRIPE_PRICE_BUSINESS=
STRIPE_PRICE_ID_STARTER=
STRIPE_SUCCESS_URL=http://localhost:3000/portal/billing?success=true
STRIPE_CANCEL_URL=http://localhost:3000/portal/billing?canceled=true
GRACE_DAYS=7

# ── Email (one of: Postmark, Resend, or SMTP) ──
# Postmark (takes precedence if set). From address must be verified in Postmark dashboard.
POSTMARK_SERVER_TOKEN=
POSTMARK_MESSAGE_STREAM=outbound
# Resend (used if POSTMARK not set):
RESEND_API_KEY=
# SMTP (used if MAIL_PROVIDER=smtp and SMTP_HOST set):
MAIL_PROVIDER=console
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
# Sender (all providers):
EMAIL_FROM=noreply@helvion.io
MAIL_FROM=noreply@helvion.io

# ── WebAuthn (Passkeys) ──
WEBAUTHN_RP_NAME=Helvino
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:3000

# ── MFA Policy ──
ADMIN_MFA_REQUIRED=false
PORTAL_MFA_RECOMMENDED=true

# ── Rate Limiting (Dev Multiplier) ──
RATE_LIMIT_DEV_MULTIPLIER=3

# ── AI Providers (set at least one) ──
OPENAI_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=

# ── Feature Flags ──
INTERNAL_OVERRIDE_WRITES=false

# ── Widget Embed CDN ──
EMBED_CDN_URL=https://cdn.helvion.io

# ── Logging ──
LOG_LEVEL=info
