# Inbox notification sound

Place `notification.mp3` here to use a custom sound when a new message arrives in the portal inbox.

If no file is present, a short beep is played instead (Web Audio API).
